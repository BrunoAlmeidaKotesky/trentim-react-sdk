import * as React from "react";
import { IDatatableProps } from '../../models/interfaces/IDataTable';
export declare const Datatable: React.MemoExoticComponent<(<T extends object>({ columns, styles, footer, rows, ignoreKeys, idName, customEvents, tableCaption, classes }: IDatatableProps<T>) => JSX.Element)>;
