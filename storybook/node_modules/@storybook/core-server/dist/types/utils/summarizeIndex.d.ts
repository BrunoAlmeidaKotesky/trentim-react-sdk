import type { Store_StoryIndex } from '@storybook/types';
export declare function summarizeIndex(storyIndex: Store_StoryIndex): {
    storyCount: number;
    docsPageCount: number;
    storiesMdxCount: number;
    mdxCount: number;
    version: number;
};
